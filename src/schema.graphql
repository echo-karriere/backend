# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: Int!
  email: String!
  password: String!
  name: String!
  avatar: String
  staff: Boolean!
  admin: Boolean!
  active: Boolean!
}

type JwtToken {
  """JWT access token"""
  accessToken: String!

  """JWT refresh token"""
  refreshToken: String!
}

type Query {
  user(id: Int!): User!
  users: [User!]!
}

type Mutation {
  login(email: String!, password: String!): JwtToken!
  changePassword(id: Int!, password: String!, newPassword: String!): User!
  change(id: Int!, email: String, name: String, avatar: String, staff: Boolean, admin: Boolean, active: Boolean): User!
}
